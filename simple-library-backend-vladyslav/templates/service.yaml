apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-library-backend-vladyslav.fullname" . }}
  labels:
    {{- include "simple-library-backend-vladyslav.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-library-backend-vladyslav.selectorLabels" . | nindent 4 }}
